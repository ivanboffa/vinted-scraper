curl_cffi        # TLS-level Chrome impersonation
asyncpg          # async PostgreSQL driver (scraper, checkers)
psycopg2-binary  # sync PostgreSQL driver (one-off queries)
python-dotenv    # .env loading
